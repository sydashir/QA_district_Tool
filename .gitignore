# Working memory / session handoff — not shared history. CLAUDE.md IS tracked.
SESSION_STATE.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Auditor runtime artifacts
cache/
reports/
spike/reports/
*.log

# Secrets / local config
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/
.idea/

# Machine-local installed tooling (Claude Code skills, settings) — not project source.
.claude/
data/
.run_in_progress
.playwright-mcp/
/dashboard.png
findings-loaded.md
.backups

# database dumps — never commit client data
backups/
